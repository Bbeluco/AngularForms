import { ValidadorCepDirective } from './validador-cep.directive';

describe('ValidadorCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidadorCepDirective();
    expect(directive).toBeTruthy();
  });
});
